<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Users</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="p-10">
      <a class="text-blue-900" href="/create">Create new User</a>
      <h1 class="text-3xl">All Users</h1>
      <hr />
      <div class="flex gap-4 flex-wrap">
        <% if(users.length > 0) { %> <% users.map((task) => { %>
        <div class="p-2 h-80 bg-neutral-200 w-56 mt-10 rounded-md">
          <div class="h-48 bg-gray-50 rounded-md">
            <img
              class="w-full h-full object-cover"
              src="<%= task.imgurl %>"
              alt=""
            />
          </div>
          <h4 class="text-xl mt-2 font-bold"><%= task.name %></h4>
          <p class="text-sm text-gray-500 italic"><%=task.email%></p>
          <div class="flex justify-between flex-wrap">
            <a class="ml-2 mt-4 font-medium" href="/edit/<%= task._id %>">Edit</a>
            <a
              class="mr-2 mt-4 text-red-800 font-medium"
              href="/delete/<%=task._id%>"
              >Delete User</a
            >
          </div>
        </div>
        <%}) %> <% } else { %>
        <h3 class="text-lg mt-8 italic">No users found</h3>
        <%}%>
      </div>
    </div>
  </body>
</html>
